<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/txtMessage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/rounded_chat_item"
        tools:text = "Engine eğitimlerinin zamanlama ve detaylarını otomatize etmek adına tasarladığımız engine streamlining altyapısı hakkında bir doküman yazacağız"
        android:paddingStart="@dimen/padding_large"
        android:paddingEnd="@dimen/padding_large"
        android:paddingTop="@dimen/margin_small"
        android:paddingBottom="@dimen/margin_small"/>

    <TextView
        android:id="@+id/txtMessageTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/txtMessage"
        app:layout_constraintRight_toRightOf="@id/txtMessage"
        android:paddingBottom="@dimen/margin_small"
        android:paddingEnd="@dimen/margin_medium"
        tools:text="18:20"/>

</androidx.constraintlayout.widget.ConstraintLayout>