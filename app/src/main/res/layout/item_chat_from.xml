<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/txtMessage"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_chat_item"
        tools:text = "This method is responsible for breaking a sentence apart into tokens. One token at a time."
        android:ellipsize="end"
        android:maxLines="10"
        android:textColor="@android:color/black"
        android:paddingStart="@dimen/padding_large"
        android:paddingEnd="@dimen/padding_large"
        android:paddingTop="@dimen/margin_small"
        android:paddingBottom="@dimen/margin_small"
        android:layout_margin="@dimen/margin_small"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0"
        app:layout_constraintHorizontal_bias="1"
        />

    <TextView
        android:id="@+id/txtMessageTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/txtMessage"
        app:layout_constraintRight_toRightOf="@id/txtMessage"
        android:paddingBottom="@dimen/margin_small"
        android:paddingEnd="@dimen/margin_medium"
        android:layout_marginTop="@dimen/margin_small"
        tools:text="18:20"/>

</androidx.constraintlayout.widget.ConstraintLayout>